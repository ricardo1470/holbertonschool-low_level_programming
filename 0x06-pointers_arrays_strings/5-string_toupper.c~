#include "holberton.h"

/**
 * string_touppe - Entry point
 * Desc: Entry
 *@*: pointer
 * Return: Always 0 (Success)
 */
char *string_toupper(char *a)
{
int i;

for (i = 0 ; a[i] != '\0' ; i++)
{
if (a[i] <= 97 && a[i] > 122)
{
a[i] - 32;
}
}
return (0);
}