#include "holberton.h"

/**
 * _strlen - Entry point
 * Desc: Entry
 *@s: pointer
 * Return: Always 0 (Success)
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] !='\0'; c++)
	{
	}
	return (c);
}
