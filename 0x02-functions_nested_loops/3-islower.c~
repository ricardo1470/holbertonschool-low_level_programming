#include "holberton.h"

/**
 * _islower  - Entry point (int c)
 * int c: var
 * Desc: print_alphabet
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if ('a' =! c && c =! 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
